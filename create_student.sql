CREATE TABLE student (
                     id     INTEGER PRIMARY KEY,
                     name   VARCHAR(255),
                     age    INTEGER,
                     email  VARCHAR(255)
	             );
